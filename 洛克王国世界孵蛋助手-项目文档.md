# 洛克王国世界 · 孵蛋助手  
## 需求与开发纪念文档

> 本文档同时覆盖**产品需求**与**技术实现**，作为项目完成后的纪念与备查。  
> 项目路径：`Desktop/rkw_hatcher`  
> 文档整理日期：2026-08-05

---

## 一、项目概述

### 1.1 定位

面向《洛克王国世界》玩家的**本地自用孵蛋助手**：管理图鉴与背包精灵，按性格 / 蛋组查询最佳配种对象，并维护「产线」与生产状态。

### 1.2 使用场景

| 场景 | 说明 |
|------|------|
| 查配种 | 指定图鉴精灵 + 性别 + 性格，快速看到可配对象与目标子代性格 |
| 管精灵 | 维护自己拥有的公 / 母个体（性格、奖章、忙闲） |
| 管图鉴 | 维护物种最佳 PVP 性格、蛋组；可从 BWIKI 同步新增 |
| 管产线 | 设定目标种 / 性格 / 奖章，按规则生成推荐方案并开停生产队 |

### 1.3 运行形态

- 电脑 / 安卓 / iOS 均可：浏览器打开即可（手机需与电脑同一局域网）
- 数据存本机 **MySQL**，非纯前端玩具库
- 前后端分离：Vue 页面 + Go API

---

## 二、需求说明

### 2.1 信息架构（四页）

| 菜单 | 路径 | 职责 |
|------|------|------|
| 最佳配种查询 | `/` | 按图鉴查询配种推荐 |
| 我的产线 | `/lines` | 产线 CRUD、方案展示、生产开停 |
| 我的精灵 | `/pets` | 背包精灵 CRUD |
| 图鉴 | `/species` | 图鉴 CRUD、Wiki 同步 |

壳层能力（各页共用）：

- **白昼 / 黑夜**主题切换（`localStorage`）
- **后端连接**状态点（未连接 / loading / 已连接；可一键启停后端 + 心跳）

### 2.2 领域约定（游戏规则抽象）

1. **子代物种随母**：推荐结果右侧「目标」物种 = 母侧物种。  
2. **性格遗传**：父母各约 50% 概率传给子代（工具不模拟随机，只用「对 / 错」表达强弱推荐）。  
3. **性格「对」**：相对「目标期望性格」或「该物种最佳 PVP 性格集合」。  
4. **奖章继承**：同类型奖章父母一致时子代才可能继承；产线入选个体须具备全部目标奖章。  
5. **可配条件**：一公一母，且至少有一个**非「无法孵蛋」**的共同蛋组。  
6. **字典只读**：蛋组 / 性格 / 奖章类型与条目仅在数据库维护，页面不可增删改字典本身。

### 2.3 最佳配种查询

**输入（全部必填）**

- 精灵：仅从图鉴选择（不是「我的精灵」）
- 性别：公 / 母
- 性格：性格字典一项

**公查（mode = stud）**

- 含义：用「该种公 + 该性格」能帮哪些同蛋组母系，产下性格正确的子代。
- 结果左侧：可作母的图鉴 / 我的母宠。
- 结果右侧：目标 = 该母种 + 目标性格。  
  - 公性格计入目标；若母个体性格也在该种最佳 PVP 内，则一并展示（如公沉默、母聪明 → 目标「沉默、聪明」）。

**母查（mode = dam）**

- 含义：为该母找合适的公。
- 若母查询性格**已是**该种最佳 PVP 之一：图鉴仍按「含查询性格」筛选；**我的精灵**则推性格落在母种最佳内的公（同性格=方案1强推，其它最佳性格=方案2弱推）。个体行右侧目标=查询性格，若公性格也在最佳内且不同则一并展示（如平和、沉默）。
- 否则：推性格落在母种最佳 PVP 内的公；右侧目标：个体行=该公性格；图鉴行=该公种最佳与母种最佳的交集（公种不具备的最佳性格不展示）。
- **图鉴行与背包无关**：同蛋组且图鉴种满足推荐性格条件即可出现（母已最佳 → 该种最佳含查询性格；母非最佳 → 与母种最佳有交集）。背包公宠另按规则附带；无匹配公时仍可只出图鉴行。

**结果展示约定**

- 「我的精灵」在前，「图鉴」在后（图鉴行半透明）。
- 方案星标对齐产线语义：  
  - **绿星（方案 1）**：公对 + 母对（强推荐）  
  - **黄星（方案 2 / 3）**：仅一侧对（弱推荐）  
  - 母查询性格不在最佳内时，母恒为「错」→ **不可能出现方案 1**。
- 仅点击「查询」后刷新结果；改表单不自动改结果区。

### 2.4 我的精灵

- 字段：物种、性别、性格、奖章（每类型至多一枚）、状态（空闲中 / 忙碌中）。
- 支持搜索、筛选、性别向排序等列表操作。
- 删除前评估影响：生产中不可删；否则提示可能被掏空的产线。

### 2.5 图鉴

- 字段：编号、名称、图标、进化链、备注、蛋组（多选）、最佳 PVP 性格（多选）。
- 列表支持名称 / 编号 / 进化链搜索；蛋组筛选（交集 / 并集）。
- **更新数据**：预览 BWIKI 差异 → 勾选提交；策略为**只新增、不覆盖**已有数据。

### 2.6 我的产线

**一条产线 = 目标模板**

- 目标物种 + 期望性格 + 目标奖章组合（同指纹不可重复建第二条）。

**推荐方案（最多四类，展示有优先级）**

| 方案 | 强度 | 含义 |
|------|------|------|
| 1 | 强 | 性格对的公 + 性格对的母 |
| 2 | 弱 | 性格错的公 + 性格对的母 |
| 3 | 弱 | 性格对的公 + 性格错的母 |
| 4 | 极弱 | 双方性格皆错，仅蛋组可配 |

规则：

- **有方案 1 则只展示方案 1**；否则可并存 2 与 3；仅当都没有时才出 4。
- 弱方案中「性格错」一侧可半透明提示。
- 无任何方案则不允许保存产线。

**生产队（runs）**

- 从方案池中选一公一母开工；双方须空闲，角色须在方案内。
- 槽位上限：`maxSlots = min(方案公数, 方案母数, 5)`；全局同时生产队数 ≤ 5。
- 开工 → 两宠「忙碌中」；停产 → 若不再参与其它队则回「空闲中」。

### 2.7 Excel（能力保留）

- 后端提供图鉴 / 精灵导入导出。
- 历史上有「设置」页；当前主导航未挂该页，API 仍可用。

### 2.8 非功能需求

- 本地优先、可局域网访问。
- 日夜主题持久化。
- 后端可从页面启停，断线有状态反馈。
- UI：柔和现代风；标签、性别按钮、方案星标等全站统一。

---

## 三、技术架构

### 3.1 总览

```
浏览器 (Vue3 SPA)
    │  HTTP /api
    ▼
Go + Gin (:3070)
    │
    ▼
MySQL · rkw_hatcher_server
```

| 目录 | 职责 |
|------|------|
| `1_front/` | Vite + Vue3 + TypeScript 前端 |
| `2_back/` | Go + Gin + MySQL 后端 |
| `2_back/sql/` | `schema.sql` 与迁移脚本 |

### 3.2 前端技术栈

- Vue `^3.5`、Vue Router `^4.6`、Vite `^5.4`、TypeScript `5.6`
- 状态：页面内 `ref` / `computed` + composables（主题、连接、字典缓存、Toast、产线方案）
- 全局样式：`1_front/src/style.css`（含 `data-theme`）

**主要入口**

- `1_front/src/main.ts` — 路由
- `1_front/src/App.vue` — 顶栏与连接 / 主题
- `1_front/src/api.ts` — API 封装
- `1_front/vite.config.ts` — 开发服 + 后端启停插件（`/__boot_backend`、`/__stop_backend`）

### 3.3 后端技术栈

- Go（`go.mod`）、Gin、`go-sql-driver/mysql`、excelize、godotenv
- 入口：`2_back/cmd/server/main.go`
- 处理器：`2_back/internal/handlers/`（species / pets / breeding / breed_schemes / excel / sync）
- 模型：`2_back/internal/models/models.go`
- 配置：`2_back/.env`（`HTTP_ADDR=:3070`，库 `rkw_hatcher_server`）

### 3.4 启动方式

1. MySQL 执行 `2_back/sql/schema.sql`（库名 `rkw_hatcher_server`）  
2. 配置 `2_back/.env`（至少 `DB_PASSWORD`、`HTTP_ADDR=:3070`）  
3. 后端：`cd 2_back && go run ./cmd/server`（也可由前端绿点拉起）  
4. 前端：`cd 1_front && npm install && npm run dev -- --host`  
5. 浏览器：`http://localhost:5173`（手机用电脑局域网 IP）

API 基址默认：`http://127.0.0.1:3070/api`（可用环境变量 `VITE_API_BASE` 覆盖）。

---

## 四、数据模型

### 4.1 字典表（页面只读）

| 表 | 说明 |
|----|------|
| `egg_groups` | 蛋组（含「无法孵蛋」） |
| `natures` | 性格（name / boost / penalty） |
| `medals` | 奖章（type + name） |

### 4.2 图鉴

| 表 | 说明 |
|----|------|
| `species` | 物种：`no`、name、icon_url、evo_chain(JSON)、notes |
| `species_best_natures` | 物种 ↔ 最佳 PVP 性格（多对多） |
| `species_egg_groups` | 物种 ↔ 蛋组（多对多） |

### 4.3 我的精灵

| 表 | 说明 |
|----|------|
| `my_pets` | species_id、gender（公/母）、nature_id、status（空闲中/忙碌中） |
| `my_pet_medals` | 个体奖章；**同一类型至多一枚** |

### 4.4 产线

| 表 | 说明 |
|----|------|
| `breeding_lines` | 名称、目标种、期望性格、状态、备注 |
| `breeding_line_medals` | 目标奖章 |
| `breeding_line_schemes` | 方案类型 1–4 |
| `breeding_line_scheme_pets` | 方案内宠快照（role = stud / dam） |
| `breeding_line_runs` | 生产队（stud_pet_id + dam_pet_id） |

**唯一性**：同目标物种 + 同期望性格 + 同奖章指纹 → 不可再建第二条产线。

---

## 五、接口一览（前缀 `/api`）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/health` | 健康检查 / 心跳 |
| GET | `/egg-groups` `/natures` `/medals` `/medal-types` | 字典 |
| GET/POST/PUT/DELETE | `/species` `/species/:id` | 图鉴 CRUD |
| POST | `/species/sync/preview` `/species/sync/commit` | Wiki 同步 |
| GET/POST/PUT/DELETE | `/pets` `/pets/:id` | 精灵 CRUD |
| GET | `/pets/:id/delete-impact` | 删宠影响预览 |
| GET/POST/PUT/DELETE | `/breeding-lines` `/breeding-lines/:id` | 产线 |
| POST | `/breeding-lines/:id/runs` | 开生产 |
| DELETE | `/breeding-lines/:id/runs/:runId` | 停生产 |
| GET | `/breed/query?speciesId&gender&natureId` | 配种查询 |
| GET/POST | `/export|import/species` `/export|import/pets` | Excel |

---

## 六、关键业务实现要点

### 6.1 配种查询 SQL 语义

- 蛋组匹配时排除 `无法孵蛋`。
- 公查：目标图鉴须在 `species_best_natures` 中包含查询性格。
- 母查：按母种最佳性格集合过滤背包公宠（母已最佳时同性格强推、其它最佳性格弱推）；图鉴行筛选规则不变，不依赖背包。

### 6.2 产线方案构建

- 核心函数：后端 `buildBreedSchemes`（与前端展示规则一致）。
- 保存产线时服务端重算方案并落库，不盲信前端传来的方案列表。
- 删宠等变更会触发方案重建；方案空则产线删除。

### 6.3 前端 UI 约定

- 弹层：`.dialog-mask` + `.dialog-card`（无单独 PopupBox 包）。
- 性别：`.gender-pick` / `.gender-btn`；列表标记 ♂/♀。
- 标签：`.meta-tag.egg` / `.nature` / `.medal…`
- 方案星：`.line-scheme-strength.strong|weak|very-weak`
- Toast：右上角通知条。

---

## 七、Excel 列约定

**图鉴**：`id, no, name, evo_chain, best_pvp_natures, egg_groups, notes`  
**精灵**：`id, species, gender, nature, medals`（奖章形如 `体型:大块头,声音:婉转音`）

---

## 八、已知差异与备忘

1. README 仍可能提到「设置」页；当前主导航为四页，`SettingsView.vue` 为孤儿页，Excel 走 API 仍可用。  
2. 配置代码默认端口曾为 `:8080`，项目约定以 **`.env` 的 `:3070`** 为准。  
3. 「性格 50/50」是游戏领域语义，代码用方案强弱表达，不做随机模拟。  
4. 配种查询结果区依赖「查询快照」，避免改表单时结果误联动。

---

## 九、目录速查

```
rkw_hatcher/
├── 1_front/                 # Vue 前端
│   ├── src/views/           # BreedQuery / Lines / Pets / Species
│   ├── src/composables/     # theme / backend / toast / dict / schemes
│   └── vite.config.ts
├── 2_back/                  # Go 后端
│   ├── cmd/server/
│   ├── internal/handlers/
│   ├── internal/models/
│   └── sql/schema.sql
├── README.md
└── 洛克王国世界孵蛋助手-项目文档.md   # 本文档
```

---

## 十、写在最后

这是一份给自己的纪念：从「想做个能查最佳配种、能管产线的本机工具」到可日夜切换、可启停后端、可同步图鉴、可开多队生产的完整小系统。

规则来自游戏，工具留在本机；文档留在这里，免得以后忘掉当时为什么绿星是强推、为什么母查沉默时出不了方案 1。

— 洛克王国世界 · 孵蛋助手 · 完 —
